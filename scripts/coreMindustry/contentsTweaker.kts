package coreMindustry

// Compatibility stub: upstream contentsTweaker implementation in current runtime
// is binary-incompatible and fails during script instantiation.
onEnable {
    logger.info("contentsTweaker compatibility stub enabled")
}
